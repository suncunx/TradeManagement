<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!--状态栏颜色-->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!--着重颜色-->
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:listDivider">@drawable/divider</item>

    </style>

    <declare-styleable name="decoration_divider">
        <attr name="boldDivider" format="dimension"/>
    </declare-styleable>
    <declare-styleable name="custom_attrs">
        <attr name="custom_attr1" format="string" />
        <attr name="custom_attr2" format="string" />
    </declare-styleable>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="MM">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
    </style>

    <style name="MW">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
    </style>

    <style name="MMVertical" parent="MM">
        <item name="android:orientation">vertical</item>
    </style>

    <style name="MWHorizontal" parent="MW">
        <item name="android:orientation">horizontal</item>
    </style>

</resources>
